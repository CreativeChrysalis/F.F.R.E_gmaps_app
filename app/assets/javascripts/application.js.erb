// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, or any plugin's
// vendor/assets/javascripts directory can be
//referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file. JavaScript code in this file should be added after the last require_* statement.
//
// Read Sprockets README (https://github.com/rails/sprockets#sprockets-directives) for details
// about supported directives.
//
//= require rails-ujs
//= require jquery
//= require_tree .

// ***anthing required here will be available throught the whole app, it is
// part of the asset pipeline

// First alert shown, on page load:


$(document).ready(function () {
    initMap();
});
function initMap() {

    //
    // var sites = [
    //   ['Intihuatana', 11.163439, -72.546732, 1],
    //   ['Machu Picchu', -13.163047, 72.544566, 4],
    //   ['Temple of the 3 Windows', -13.163726, -72.544881, 5],
    //   ['House of the Guardian to the Funerary Rock', -13.165398, 33.9234234, 3],
    //   ['Sacred Rock', -13.161846, 76.546104, 2]
    //
    // ];

    var latLong = new google.maps.Map(document.getElementById('map'));
    var image = "<%= asset_path ('unicorn_icon')%>";
    // var image = "<%= asset_path ('unicorn_icon') %>";
    var mapOptions = {
      zoom: 1,
     center: {lat: -34.397, lng: 150.644},
      scrollwheel: false
    };
    var map = new google.maps.Map(document.getElementById('map'), mapOptions);
    // sets marker
    // var marker = new google.maps.Marker({
    //   position: latLong,
    //   map: map,
    //   title: 'Stronbox West',
    //   icon: image,
    //   animation: google.maps.Animation.BOUNCE
    // });



    // for (i = 0; i < sites.length; i++) {
    // for (locations) {
    //   console.log("marker added");
    //     marker = new google.maps.Marker({
    //         // position: new google.maps.LatLng(sites[i][1], sites[i][2]),
    //        position: latLong,
    //         map: map,
    //         icon: image,
    //         animation: google.maps.Animation.DROP
    //     });



    var marker = new google.maps.Marker({
        position: {lat: -34.397, lng: 150.644},
         icon: image,
      animation: google.maps.Animation.DROP
    });

    // To add the marker to the map, call setMap();
    marker.setMap(map);


        var infowindow = new google.maps.InfoWindow({
           content: location.description
        });

        marker.addListener('click', function() {
          infowindow.open(map, marker);
        });
    }











    // google.maps.event.addListener(marker, 'click', function() {
    //    infowindow.open(map,marker);
    // });
    //
    // marker.addListener('click', function() {
    //   infowindow.open(map, marker);
    // });
// }
